export type Sticker = {
  id: string;
  src: string;
  x: number;
  y: number;
  width: number;
  height: number;
  rotation: number;
};

export type StickerMeta = {
  id: string;
  category: string;
  label: string;
};
